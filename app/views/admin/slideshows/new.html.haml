#slideshow-page
  - form_for(:slideshow, :url => "/admin/slideshows/create", :html => {:multipart => true}) do
    %h2
      Select a Parent Page for the Slideshow
    = select(:slideshow, :page_id, @children.all.collect {|p| [p.title, p.id]})
    %em
      This slideshow will also appear on the child pages of the parent.
    %p.buttons
      = save_model_button(@slideshow)
      or
      = link_to "Cancel", {:action => :index}